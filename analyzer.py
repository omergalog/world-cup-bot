import anthropic
import os
from dotenv import load_dotenv

load_dotenv()

client = anthropic.Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

SYSTEM_PROMPT = """אתה סוכן הימורים מקצועי המתמחה בניתוח משחקי מונדיאל 2026.
תפקידך לנתח כל משחק ולחזות את התוצאה המדויקת (למשל 2:1, 0:0).

כללי הניתוח שלך:
1. חפש באינטרנט מידע עדכני על שתי הקבוצות לפני הניתוח
2. בדוק: מצב שחקנים ופצועים, הרכב אחרון, 5 משחקים אחרונים, היסטוריית עימותים
3. שקול: xG (שערים צפויים), כושר בית/חוץ, עייפות, לחץ טקטי
4. גזור את התוצאה המדויקת מהנתונים - אסור ברירת מחדל! (ראה עיקרון ז')
5. הסבר את התחזית ב-5 משפטים בעברית בלבד

עקרונות קריטיים (לקחים מנותחים מ-11 משחקי מונדיאל 2026 - חובה ליישם בשיקול דעת, לא כנוסחה!):
חשוב: אין "ברירת מחדל". כל משחק נשקל לגופו. הכללים הבאים הם גורמים שצריך לשקלל - לא כללים אוטומטיים.

א. תסמונת משחק פתיחה (דפוס חזק בטורניר זה): פייבוריטיות רבות מגמגמות במשחק הראשון - שולטות אך לא כובשות (דוגמאות: ספרד 27 בעיטות→0, שווייץ 3.24 xG→שער אחד). אל תניח אוטומטית שעליונות בשליטה = ניצחון.

ב. עגן את ההכרעה להסתברות בפועל (Opta / שוק ההימורים) - זה העוגן הכי חשוב:
   • פייבוריטית מעל ~60% לניצחון → חזה ניצחון, גם אם יש סימן אזהרה שולי. חלק מהמשחקים האלה יסתיימו בתיקו בגלל שונות (מזל) - זה בלתי נמנע ואי אפשר לחזות זאת. אל תיתן תיקו רק כי "פעם הופתעו".
   • משחק קוין-פליפ אמיתי (פייבוריטית ~40-45%, תיקו ~25%+) → כאן תיקו הוא תחזית לגיטימית, אבל לא אוטומטית! גם במשחק צמוד יש בדרך כלל צד עם יתרון קל - זהה אותו (כושר, הרכב, מוטיבציה, מי שצריך ניצחון, יתרון ביתי) והכרע לגופו. הרבה משחקים צמודים נגמרים בניצחון מינימלי (1-0, 2-1) ולא בתיקו. תן תיקו רק כששני הצדדים באמת שקולים ואין שום הטיה מכרעת.
   הבחנה קריטית - אבל זהירות: בתחילת הטורניר חשבנו ששוקים כמו ספרד 0-0 הם "שונות בלבד". התברר שזה **דפוס חוזר** (ראה עיקרון ח') - פייבוריטית בזבזנית מול גוש+שוער חם. לכן: אפילו פייבוריטית של 65-87% שחסרה חדות התקפית מול גוש מאורגן - תרחיש השוק/תיקו ריאלי וצריך להישקל ברצינות, לא להידחות כ"שונות". מנגד, פייבוריטית חדה ומלאה (ספרד עם יאמל 4-0) - תנצח. שקול את החדות ההתקפית בפועל.

ג1. סימן אזהרה ממשי (כוכב פצוע, הגנה רעועה, הכנה כאוטית) מוריד את הביטחון - אבל שקלל אותו מול עוצמת הפער. סימן אזהרה שולי מול פער ענק לא הופך משחק לתיקו.

ג. אנדרדוגים מאורגנים מסוכנים: נבחרת עם גוש הגנתי, שוער חם וקונטרות גונבת נקודות - במיוחד במשחקי פתיחה.

ד. xG ודירוג FIFA = "מי טוב יותר", לא "מי ינצח היום". שליטה ושליטה ב-xG לא שווה שערים.

ה. תיקו היא תחזית לגיטימית ושכיחה בטורניר זה - אבל תן אותה רק כשהניתוח מצדיק (סימני אזהרה / משחק צמוד אמיתי), לא כברירת מחדל ולא מפחד. אם פייבוריטית באמת חזקה ונקייה מסימני אזהרה - תן לה לנצח בביטחון.

ו. בכל ניתוח: זהה במפורש האם יש סימני אזהרה לכל צד, שקלל אותם, ורק אז הכרע. הסבר בניתוח מדוע בחרת ניצחון/תיקו לאור הגורמים האלה.

ח. דפוס "הפייבוריטית הבזבזנית" (חזק מאוד בטורניר זה - נצפה 5+ פעמים: ספרד 0-0 כף ורדה, אקוודור 0-0 קוראסאו, בלגיה 0-0 איראן, אורוגוואי 2-2 כף ורדה, טורקיה). זהו כלי לחשיבה עמוקה, לא מתג אוטומטי:
   • כשפייבוריטית **חסרת יוצרים/חוד מרכזי, או בזבזנית בכושר, או חסרת מוטיבציה**, פוגשת **גוש הגנתי מאורגן עם שוער בכושר** → אל תניח שהשליטה תתורגם לשערים. שקול ברצינות רבה תרחיש שוק/תיקו (0-0, 1-1) גם אם הפייבוריטית מדורגת גבוה. זה קרה שוב ושוב.
   • לעומת זאת, כשלפייבוריטית יש **מלוא הנשק ההתקפי + חדות + מוטיבציה** (דוגמה: ספרד עם יאמל ניצחה 4-0 אחרי שבלעדיו עשתה 0-0; מצרים עם סלאח; גרמניה; צרפת) → היא כן פורצת ומנצחת בביטחון.
   • ההבדל המכריע הוא **הנשק ההתקפי והחדות בפועל**, לא הדירוג. בדוק תמיד: האם הפייבוריטית מעמידה את מיטב התקפתה והאם היא חדה בכושר?
   • זה לא אומר "תמיד תיקו מול גוש" ולא "תמיד ניצחון לחזקה" - זה אומר לנתח לעומק את החדות ההתקפית מול איכות הגוש והשוער, ולהכריע לגופו. אם הניתוח מצביע על שוק - אל תרשום ניצחון מתוך כבוד לדירוג; אם הוא מצביע על פריצה - אל תפחד לתת ניצחון רחב.

ז. גזירת התוצאה המדויקת - אסור ברירת מחדל ואסור "תפריט" תוצאות קבוע! חשב כל משחק מאפס:
   • חשב בנפרד כמה שערים **כל קבוצה** צפויה לכבוש - כמספר חופשי (0,1,2,3,4,5,6...). אל תבחר מתוך רשימה סגורה של תוצאות "מותרות".
   • שקלל לכל צד: ה-xG ההתקפי שלו, איכות/חולשת ההגנה והשוער של היריב, פצועים והרכבים בפועל, כושר ההבקעה/הספיגה האחרון, סגנון משחק (התקפי/הגנתי), קצב, מזג אוויר/גובה, ומשמעות המשחק.
   • שקול את כל התרחישים: קבוצה חזקה מאוד מול חלשה יכולה לכבוש 4-5-6; קבוצה חלשה יכולה לחטוף שער כבוד גם בהפסד רחב; משחק יכול להיגמר 0-0, 3-3, 4-2, 1-0, 5-1 - כל תוצאה אפשרית אם הנתונים מובילים אליה.
   • אל תחזור על אותה תוצאה ממשחק למשחק מתוך נוחות. כל תוצאה חייבת לנבוע מהחישוב הספציפי של אותו משחק. אם אתה מוצא את עצמך כותב את אותה תוצאה שוב ושוב - עצור, זה סימן שאתה לא באמת מחשב.

פורמט ההודעה (RTL, עברית):
🏆 [שם קבוצה א'] 🆚 [שם קבוצה ב']
📅 [תאריך ושעה]
🎯 תחזית: [X:Y לטובת קבוצה / תיקו]

[5 משפטי הסבר בעברית]

חובה! בסוף כל ניתוח חייב להופיע:
🎯 תחזית סופית: [X:Y] לטובת [קבוצה] / תיקו [X:X]
אל תסיים בלי לתת תוצאה מדויקת!

⚠️ ניתוח ראשוני / ניתוח מעודכן לאחר הרכבים"""

WEB_SEARCH_TOOL = {
    "type": "web_search_20250305",
    "name": "web_search",
    "max_uses": 5
}


def analyze_match(team1: str, team2: str, match_time: str, with_lineups: bool = False) -> str:
    lineup_note = "הרכבים הרשמיים פורסמו - עדכן את הניתוח בהתאם." if with_lineups else "זהו ניתוח ראשוני לפני פרסום הרכבים הרשמיים."

    prompt = f"""חפש באינטרנט מידע על: {team1} vs {team2} ({match_time}).
{lineup_note}
אסוף: xG, שערים, פצועים, H2H, דירוג FIFA.

לאחר החיפוש, כתוב את הפלט הבא בדיוק - לא יותר ולא פחות:

🏆 {team1} 🆚 {team2}
📅 {match_time}

• [עובדה 1]
• [עובדה 2]
• [עובדה 3]
• [עובדה 4]
• [עובדה 5]

🎯 תחזית סופית:
X - {team1}
Y - {team2}

חובה: החלף את X ו-Y במספרים אמיתיים. זהו הפלט המלא - אל תוסיף שום דבר אחר."""

    response = client.beta.messages.create(
        model="claude-opus-4-8",
        max_tokens=2000,
        system=SYSTEM_PROMPT,
        messages=[{"role": "user", "content": prompt}],
        tools=[WEB_SEARCH_TOOL],
        betas=["web-search-2025-03-05"],
    )

    result = ""
    for block in response.content:
        if hasattr(block, "text"):
            result += block.text

    # אם הניתוח לא מכיל תחזית, הוסף הודעת שגיאה
    if "🎯" not in result:
        result += f"\n\n🎯 לא ניתן היה לחזות את תוצאת {team1} - {team2}"

    return result or "לא ניתן היה לנתח את המשחק כרגע."


class MatchFetchError(Exception):
    """נזרק כשקריאת ה-API נכשלת (למשל אין קרדיט) - להבדיל מ'אין משחקים'"""
    pass


def _fetch_matches(prompt: str, default_date: str) -> list:
    """שולח prompt למודל ומפענח רשימת משחקים בפורמט JSON.
    מחזיר רשימה (ייתכן ריקה אם באמת אין משחקים).
    זורק MatchFetchError אם קריאת ה-API עצמה נכשלה."""
    try:
        response = client.beta.messages.create(
            model="claude-opus-4-8",
            max_tokens=3000,  # מספיק מקום גם אחרי חיפוש וחישובי המרת שעות, כדי שה-JSON לא ייחתך
            messages=[{"role": "user", "content": prompt}],
            tools=[WEB_SEARCH_TOOL],
            betas=["web-search-2025-03-05"],
        )
    except Exception as e:
        # שגיאת API אמיתית (קרדיט/רשת/הרשאה) - לא לבלוע בשקט!
        print(f"❌ שגיאת API בשליפת משחקים: {e}")
        raise MatchFetchError(str(e))

    import json
    for block in response.content:
        if hasattr(block, "text"):
            text = block.text.strip()
            if "[" in text:
                start = text.index("[")
                end = text.rindex("]") + 1
                try:
                    matches = json.loads(text[start:end])
                except Exception:
                    continue
                for m in matches:
                    if not m.get("date"):
                        m["date"] = default_date
                return matches

    # לא נמצא JSON - לוג לאבחון (חיתוך? תשובה ריקה?)
    print(f"⚠️ לא נמצא JSON בתשובה. stop_reason={getattr(response, 'stop_reason', '?')}")
    return []


def get_upcoming_matches(hours: int = 24) -> list:
    """מחזיר את כל משחקי המונדיאל שמתחילים בין עכשיו ל-N השעות הקרובות (שעון ישראל).
    שואל לפי חלון זמן מפורש כדי לתפוס נכון משחקי לילה שמתחילים אחרי חצות בישראל."""
    from datetime import datetime, timedelta
    from zoneinfo import ZoneInfo
    now = datetime.now(ZoneInfo("Asia/Jerusalem"))
    until = now + timedelta(hours=hours)
    now_s = now.strftime("%d/%m/%Y %H:%M")
    until_s = until.strftime("%d/%m/%Y %H:%M")

    prompt = f"""חפש באינטרנט את כל משחקי מונדיאל 2026 שמתחילים בין {now_s} ל-{until_s} בשעון ישראל.
⚠️ קריטי: משחקים שמתקיימים בארה"ב/קנדה/מקסיקו בערב המקומי מתחילים אחרי חצות בשעון ישראל (01:00-07:00 לפנות בוקר) ושייכים לתאריך הישראלי של היום שאחרי. אל תפספס אותם!
לדוגמה: משחק ב-22:00 בחוף המזרחי של ארה"ב = 05:00 למחרת בשעון ישראל.

החזר JSON בלבד בפורמט:
[{{"team1": "שם קבוצה 1", "team2": "שם קבוצה 2", "date": "DD/MM/YYYY", "time": "HH:MM"}}]
שדה date ו-time חייבים להיות בשעון ישראל המדויק של תחילת המשחק.
אם אין משחקים בטווח, החזר []. החזר JSON בלבד, ללא טקסט נוסף."""

    return _fetch_matches(prompt, now.strftime("%d/%m/%Y"))


def get_todays_matches(date_str: str = None) -> list:
    """מחזיר רשימת משחקים של תאריך נתון (ברירת מחדל: היום בשעון ישראל)"""
    from datetime import datetime
    from zoneinfo import ZoneInfo
    if date_str is None:
        date_str = datetime.now(ZoneInfo("Asia/Jerusalem")).strftime("%d/%m/%Y")

    prompt = f"""מה משחקי מונדיאל 2026 בתאריך {date_str} בשעון ישראל?
חפש באינטרנט ותחזיר רשימה של משחקים בפורמט הבא בלבד (JSON):
[{{"team1": "שם קבוצה 1", "team2": "שם קבוצה 2", "date": "DD/MM/YYYY", "time": "HH:MM"}}]
השתמש בשעון ישראל (IL) — משחק שמתחיל אחרי חצות בשעון ישראל שייך לתאריך של היום הבא.
אם אין משחקים, החזר רשימה ריקה: []
החזר JSON בלבד, ללא טקסט נוסף."""

    return _fetch_matches(prompt, date_str)


def analyze_daily_matches(matches: list) -> str:
    matches_text = "\n".join([f"- {m['team1']} נגד {m['team2']} בשעה {m['time']}" for m in matches])

    prompt = f"""נתח את משחקי מונדיאל 2026 הבאים בלבד - אל תוסיף משחקים אחרים ואל תחפש משחקים נוספים:
{matches_text}

לכל משחק ברשימה: חפש באינטרנט מידע עדכני (xG, פצועים, H2H, דירוג FIFA) ובצע ניתוח מקיף עם תחזית תוצאה מדויקת.
הצג כל משחק בפורמט הנכון עם דגלי הנבחרות והשעה בשעון ישראל."""

    response = client.beta.messages.create(
        model="claude-opus-4-8",
        max_tokens=4096,
        system=SYSTEM_PROMPT,
        messages=[{"role": "user", "content": prompt}],
        tools=[{**WEB_SEARCH_TOOL, "max_uses": 8}],
        betas=["web-search-2025-03-05"],
    )

    result = ""
    for block in response.content:
        if hasattr(block, "text"):
            result += block.text

    return result or "לא ניתן היה לנתח את המשחקים כרגע."
